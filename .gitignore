# Study materials — reference footage, stills, copyrighted frames. Local-only.
references/

# Local Claude Code settings (machine-specific)
.claude/

# Krita / editor clutter
*.kra~
*.kra-autosave.kra
*~

# OS noise
.directory
Thumbs.db
.DS_Store

# intermediate render scratch (PNG sequences); the .gif is the artifact
.render/

# Krita image-sequence output (one file per frame) — the single .gif is the artifact
progress/**/frame[0-9]*.png
progress/**/frame[0-9]*.gif
progress/**/*.gif.log
